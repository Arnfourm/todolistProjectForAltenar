apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-backend.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
