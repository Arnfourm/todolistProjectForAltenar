apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todolistchart.fullname" . }}-test-connection"
  labels:
    {{- include "todolistchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todolistchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
