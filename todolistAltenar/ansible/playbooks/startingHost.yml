---
- name: Simple tool for starting host
  hosts: linuxSystems
  become: true

  tasks:
  - name: Download python
    ansible.builtin.raw: dnf install python3 python3-pip

  - name: Update && upgrade
    ansible.builtin.dnf:
      name: "*"
      state: latest
      update_cache: yes