# Docker
# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- main

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'
  dockerhub-registry: luverk1d/todolist-registry-test

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      name: luver-pool
    steps:
    - task: Docker@2
      displayName: Login into docker account
      inputs:
        command: login
        containerRegistry: $(dockerhub-registry)

    - task: Docker@2
      displayName: Build an image
      inputs:
        command: build
        dockerfile: '$(Build.SourcesDirectory)/todolistAltenar/backend/dockerfile'
        containerRegistry: $(dockerhub-registry)
        tags: |
          $(tag)